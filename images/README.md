# Images

Drop a screenshot in with the exact filename below and the readme picks it up — no editing
needed. The files here now are placeholders; overwrite them.

| File | What to capture |
|---|---|
| `screenshot-canvas.png` | The hero shot at the top of the readme. A workspace with a few agents, group zones and at least one signal arrow in flight. |
| `screenshot-split.png` | Two workspaces open side by side, with the divider visible. |
| `screenshot-inspector.png` | A selected agent card — selection ring, hover toolbar — with the inspector panel open on the right. |
| `screenshot-terminal.png` | An agent card with a live CLI running inside it. |
| `screenshot-history.png` | The History tab: signal queue plus audit log. |
| `logo.png` | The mark used in the readme header and as the dashboard favicon. Already in place. |

Capture at 1600×900 or wider. Dark theme reads better against GitHub's default, but either
works — the readme makes no assumption.

Two things to check before committing a screenshot: it is a **public** repo, so no client names,
no real paths you would rather not publish, and no readable secrets in a terminal pane.

`ui.jpg` is the old dashboard from before the UI rework. Nothing references it — delete it once
`screenshot-canvas.png` is real.
